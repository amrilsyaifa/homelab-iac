apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: balasiq

spec:
  selector:
    app: balasiq-frontend

  ports:
    - name: http
      port: 80
      targetPort: 80

  type: ClusterIP