apiVersion: v1
kind: Service

metadata:
  name: landing
  namespace: balasiq

spec:
  selector:
    app: balasiq-landing

  ports:
    - name: http
      port: 80
      targetPort: 80

  type: ClusterIP